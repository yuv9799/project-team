'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {Search,SlidersHorizontal,MapPin,Clock3} from 'lucide-react';import {api} from '../../lib/api';
export default function Discover(){const [q,setQ]=useState(''),[users,setUsers]=useState<any[]>([]),[loading,setLoading]=useState(true);async function load(){setLoading(true);try{setUsers(await api('/api/users'+(q?`?q=${encodeURIComponent(q)}`:'')))}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <main><div className="section py-9"><div><div className="pill">Student network</div><h1 className="mt-3 text-4xl font-black tracking-tight">Discover teammates</h1><p className="mt-2 max-w-2xl text-[#667085]">Search students by skills, interests and experience. Build a team that complements what you already know.</p></div><div className="card mt-7 p-3"><div className="flex flex-col gap-2 md:flex-row"><div className="relative flex-1"><Search className="absolute left-4 top-3.5 text-[#98a2b3]" size={18}/><input className="input pl-11" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search by name, skill, branch or interest..."/></div><button className="btn btn-primary" onClick={load}><Search size={17}/> Search</button><button className="btn"><SlidersHorizontal size={17}/> Filters</button></div></div><div className="mt-6 flex items-center justify-between"><div className="text-sm font-semibold text-[#667085]">{users.length} students found</div><div className="text-xs text-[#98a2b3]">Verified college profiles</div></div>{loading?<div className="mt-5 grid gap-4 md:grid-cols-3">{[1,2,3].map(x=><div className="card animate-pulse p-5" key={x}><div className="h-12 w-12 rounded-full bg-[#eef0f5]"/><div className="mt-4 h-5 w-36 rounded bg-[#eef0f5]"/><div className="mt-3 h-4 w-48 rounded bg-[#eef0f5]"/></div>)}</div>:<div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{users.map(u=><div className="card card-hover p-5" key={u.id}><div className="flex items-start gap-3"><div className="avatar h-12 w-12 shrink-0">{u.name?.split(' ').map((x:string)=>x[0]).join('').slice(0,2)}</div><div className="min-w-0 flex-1"><div className="font-black">{u.name}</div><div className="mt-1 text-xs text-[#667085]">{u.branch} · Year {u.year}</div></div><span className="pill pill-green">Available</span></div><p className="mt-4 line-clamp-2 text-sm leading-6 text-[#667085]">{u.bio||'Building projects, learning new skills and looking for great collaborators.'}</p><div className="mt-4 flex flex-wrap gap-1.5">{u.skills?.slice(0,5).map((s:any)=><span className="pill pill-gray" key={s.id}>{s.name}</span>)}</div><div className="mt-4 flex items-center gap-3 text-xs text-[#667085]"><span className="flex items-center gap-1"><Clock3 size={13}/> 6–10 PM</span><span className="flex items-center gap-1"><MapPin size={13}/> {u.college||'College'}</span></div><Link className="btn mt-5 w-full" href={`/profile/${u.id}`}>View profile</Link></div>)}</div>}</div></main>}
