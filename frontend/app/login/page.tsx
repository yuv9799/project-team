'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {LockKeyhole,Mail,Users} from 'lucide-react';import {api,saveAuth} from '../../lib/api';
export default function Login(){const [email,setEmail]=useState('aarav@kiit.edu'),[password,setPassword]=useState('password123'),[error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:any){e.preventDefault();setBusy(true);setError('');try{saveAuth(await api('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})}));router.push('/dashboard')}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="min-h-[calc(100vh-68px)] grid place-items-center px-5 py-12"><div className="w-full max-w-5xl grid overflow-hidden rounded-3xl border border-[#e7e9f2] bg-white shadow-[0_25px_70px_rgba(16,24,40,.09)] md:grid-cols-2"><div className="grid-bg hidden bg-[#f7f7ff] p-10 md:block"><div className="flex items-center gap-2 font-black"><span className="avatar h-9 w-9">CC</span>CampusConnect</div><div className="mt-24 max-w-sm"><div className="pill">Welcome back</div><h2 className="mt-4 text-4xl font-black leading-tight">Continue building with people who get your idea.</h2><p className="mt-4 leading-7 text-[#667085]">Your skills, availability and project experience power better teammate recommendations.</p></div></div><div className="p-7 sm:p-10"><div className="mx-auto max-w-md"><div className="mb-8 md:hidden"><div className="flex items-center gap-2 font-black"><span className="avatar h-9 w-9">CC</span>CampusConnect</div></div><h1 className="text-3xl font-black">Welcome back</h1><p className="mt-2 text-[#667085]">Sign in to your student workspace.</p><form onSubmit={submit} className="mt-7 space-y-4"><label className="block text-sm font-bold">College email<div className="relative mt-2"><Mail className="absolute left-3.5 top-3.5 text-[#98a2b3]" size={17}/><input className="input pl-10" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@college.edu"/></div></label><label className="block text-sm font-bold">Password<div className="relative mt-2"><LockKeyhole className="absolute left-3.5 top-3.5 text-[#98a2b3]" size={17}/><input className="input pl-10" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></div></label>{error&&<div className="rounded-xl bg-[#fff1f0] p-3 text-sm text-[#b42318]">{error}</div>}<button disabled={busy} className="btn btn-primary w-full py-3">{busy?'Signing in...':'Sign in'}</button></form><div className="mt-6 flex items-center gap-3 text-xs text-[#98a2b3]"><div className="h-px flex-1 bg-[#eef0f5]"/>or<div className="h-px flex-1 bg-[#eef0f5]"/></div><Link href="/register" className="btn mt-5 w-full">Create a new account</Link><p className="mt-5 text-center text-xs text-[#98a2b3]">Use your college identity to keep the student network trusted.</p></div></div></div></main>}
