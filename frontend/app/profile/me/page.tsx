'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {api} from '../../../lib/api';
export default function MyProfile(){const [u,setU]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{api('/api/users/me').then(setU).catch(e=>setError(e.message))},[]);if(error)return <main className="section py-16"><div className="card p-10 text-center"><h1 className="text-2xl font-black">Sign in to view your profile</h1><Link className="btn btn-primary mt-5" href="/login">Login</Link></div></main>;if(!u)return <main className="section py-16"><div className="card p-10">Loading...</div></main>;return <main><div className="section py-9"><div className="card p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="avatar h-20 w-20 text-xl">{u.name?.split(' ').map((x:string)=>x[0]).join('').slice(0,2)}</div><div className="flex-1"><div className="pill">My profile</div><h1 className="mt-2 text-3xl font-black">{u.name}</h1><p className="mt-1 text-[#667085]">{u.branch} · Year {u.year} · {u.college}</p></div><Link href="/onboarding" className="btn">Edit profile</Link></div><div className="mt-7 border-t border-[#eef0f5] pt-6"><h2 className="font-black">Skills</h2><div className="mt-3 flex flex-wrap gap-2">{u.skills?.map((s:any)=><span className="pill" key={s.id}>{s.name}</span>)}</div><h2 className="mt-7 font-black">About</h2><p className="mt-2 max-w-3xl leading-7 text-[#667085]">{u.bio||'Add a short bio from onboarding so teammates know what you enjoy building.'}</p></div></div></div></main>}
