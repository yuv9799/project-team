'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';import {api,saveAuth} from '../../lib/api';
export default function Register(){const [form,setForm]=useState({name:'',email:'',password:'password123',college:'KIIT University',branch:'CSE',year:2});const [error,setError]=useState('');const [busy,setBusy]=useState(false);const router=useRouter();const set=(k:string,v:any)=>setForm({...form,[k]:v});async function submit(e:any){e.preventDefault();setBusy(true);try{saveAuth(await api('/api/auth/register',{method:'POST',body:JSON.stringify(form)}));router.push('/onboarding')}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="min-h-[calc(100vh-68px)] px-5 py-10"><div className="mx-auto max-w-3xl"><div className="text-center"><div className="pill">Step 1 of 2</div><h1 className="mt-4 text-4xl font-black">Create your student profile</h1><p className="mt-2 text-[#667085]">It takes less than two minutes to get your first recommendations.</p></div><form onSubmit={submit} className="card mt-8 p-7 sm:p-9"><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-bold">Full name<input className="input mt-2" required value={form.name} onChange={e=>set('name',e.target.value)} placeholder="Your name"/></label><label className="text-sm font-bold">College email<input className="input mt-2" required type="email" value={form.email} onChange={e=>set('email',e.target.value)} placeholder="you@college.edu"/></label><label className="text-sm font-bold">College<input className="input mt-2" value={form.college} onChange={e=>set('college',e.target.value)}/></label><label className="text-sm font-bold">Branch<input className="input mt-2" value={form.branch} onChange={e=>set('branch',e.target.value)}/></label><label className="text-sm font-bold">Year<input className="input mt-2" type="number" min="1" max="6" value={form.year} onChange={e=>set('year',Number(e.target.value))}/></label><label className="text-sm font-bold">Password<input className="input mt-2" type="password" required value={form.password} onChange={e=>set('password',e.target.value)}/></label></div>{error&&<div className="mt-5 rounded-xl bg-[#fff1f0] p-3 text-sm text-[#b42318]">{error}</div>}<button disabled={busy} className="btn btn-primary mt-7 w-full py-3">{busy?'Creating profile...':'Continue to profile setup'}</button><p className="mt-5 text-center text-sm text-[#667085]">Already have an account? <Link className="font-bold text-[#5b5bd6]" href="/login">Sign in</Link></p></form></div></main>}
