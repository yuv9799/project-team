'use client';
import {useEffect,useState} from 'react';import {api} from '../../lib/api';import {Check,Clock3,X} from 'lucide-react';
export default function Requests(){const [rows,setRows]=useState<any[]>([]),[error,setError]=useState('');async function load(){try{setRows(await api('/api/projects/requests/mine'))}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);async function act(id:number,status:string){try{await api(`/api/projects/requests/${id}`,{method:'PATCH',body:JSON.stringify({status})});load()}catch(e:any){setError(e.message)}}return <main><div className="section py-9"><div className="pill">Collaboration inbox</div><h1 className="mt-3 text-4xl font-black">Requests</h1><p className="mt-2 text-[#667085]">Manage invitations and start collaborations without exposing personal contact details.</p>{error&&<div className="mt-5 rounded-xl bg-[#fff1f0] p-3 text-sm text-[#b42318]">{error}</div>}<div className="mt-7 space-y-3">{rows.map(r=><div className="card p-5" key={r.id}><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-start gap-3"><div className="avatar h-11 w-11">CC</div><div><div className="font-black">Project #{r.project_id}</div><div className="mt-1 text-sm text-[#667085]">Collaboration request · {r.status}</div><p className="mt-2 text-sm">{r.message}</p></div></div><div className="flex items-center gap-2">{r.status==='PENDING'?<><button className="btn btn-primary" onClick={()=>act(r.id,'ACCEPTED')}><Check size={16}/> Accept</button><button className="btn" onClick={()=>act(r.id,'REJECTED')}><X size={16}/> Decline</button></>:<span className="pill pill-gray"><Clock3 size={13}/>{r.status}</span>}</div></div></div>)}{rows.length===0&&<div className="card p-12 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-[#efefff] text-[#5b5bd6]"><Check size={25}/></div><h3 className="mt-4 text-lg font-black">You're all caught up</h3><p className="mt-1 text-sm text-[#667085]">New collaboration requests will appear here.</p></div>}</div></div></main>}
